const SUCСESSFUL_REQUEST = 200;
const CREATED = 201;
const URL_PATTERN = /^https?:\/\/(?:w{3}\.)?(?:[a-z0-9]+[a-z0-9-]*\.)+[a-z]{2,}(?::[0-9]+)?(?:\/\S*)?#?$/i;

module.exports = {
  SUCСESSFUL_REQUEST,
  CREATED,
  URL_PATTERN,
};
